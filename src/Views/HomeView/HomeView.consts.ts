enum HomeViewTitleNames {
  map = "tabs.transport.map.title",
  info = "tabs.transport.driver_info.title",
}

enum HomeViewOther {
  button = "back_btn",
}

export { HomeViewTitleNames, HomeViewOther };
