// aren't that many common consts, so no point moduling it

export enum TransportTypeEnum {
  all = "all",
  cargo = "cargo",
  public = "public",
  special = "special",
}

export enum TransportViewStackNameEnum {
  vehicle = "TransportMap",
  info = "TransportInfo",
}

export const APP_TITLE = "TransportCo";
