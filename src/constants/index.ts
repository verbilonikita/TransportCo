// aren't that many common consts, so no point moduling it

export enum TransportTypeEnum {
  all = "all",
  cargo = "cargo",
  public = "public",
  special = "special",
}

export enum TransportStackNameEnum {
  info = "transport-info",
  transport = "transport-stack",
}

export const APP_TITLE = "TransportCo";
